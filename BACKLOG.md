# Знайдені вади карти, ще не виправлені

Аудит 20 вересня 2026. Приводом була скарга оператора: «на карте плохо
отображается волгоградская область, все точки где упоминаются районы ставятся
просто на город». Скаргу закрито (див. «Що вже зроблено»), але по дорозі
знайшлось іще одинадцять речей. Тут вони з числами, причинами й пропозиціями —
щоб братись за них у новій сесії без цього контексту.

**Числа заміряні на вікні сховища (з 2026-04-18, 211 183 події) станом на
коміт `af01263`.** Після будь-якої правки вони зсунуться — перемірювати.

---

## Що вже зроблено (щоб не переробляти)

| коміт | що |
|---|---|
| `6dabbc9` | назва області («Волгоградская») більше не стає маркером-містом |
| `4a317e6` | виправлення після критичної перевірки: свіжі доби, ключ кешу жару, числа |
| `f8b62e1` | маркер області більше не зʼїдає прикметник району («Россошанский») |
| `a0ea78b` | маркер гаситься за спільною позицією сутностей, а не повторною перевіркою тексту |

Головна міра продукту — частка ТОЧКОВИХ подій, намальованих крапкою на центрі
області: було **15.9%**, стало **12.7%** (7416 -> 5958 подій).

---

## 1. Привʼязка до цілей — ЗРОБЛЕНО НАВПАКИ (20 вересня, попап карти)

**Пункт переписано після аудиту. Початкова пропозиція (привʼязувати ще й
`conf="region"`) виявилась шкідливою — заміри нижче.**

Премиса була вірна: `conf="region"` ([geocode.py:307](tgmine/geocode.py:307))
не фолбек, а розвʼязання топоніма ВСЕРЕДИНІ названої області, і тримати його в
`REGIONAL_FALLBACK` неправильно. Але числа з першої редакції (83% справжніх НП)
міряні на СУТНОСТЯХ за 31 добу. У точковому шарі, який годує цілі, розклад
інший: ADM\* 14 275 (46.5%), PPL\* 16 446 (53.5%).

**Чому розширення привʼязки шкідливе.** Симуляція на сховищі (211 183 події):
PPL-привʼязка додала б 8508 подій і винесла б у топ-10 обʼєкти, чий жар — один
центроїд містечка: «Військовий обʼєкт (Aleksin)» 301 із 301 з топоніма
`Aleksin`, «Chertkovo» 228 із 230. Це та сама вада, що вже була в Клокового,
лише масштабом містечка.

**Бо метрика зламана рівнем вище.** Заміряно на 7301 наявній привʼязці:

| міра | значення |
|---|---|
| привʼязок із `geo_conf="city-marker"` | 6422 (88%) |
| медіана частки жару з ОДНОГО топоніма | 100% |
| цілей (≥20 подій), де ≥80% жару з одного топоніма | 88% |
| текст події називає обʼєкт (НПЗ/аеродром/склад) | 86 із 7301 (1.2%) |
| унікальних топонімів на всі привʼязки | 312 |
| подій, де в радіусі ≥6 цілей | 68.3% |
| подій, де нічия в тому самому ярусі | 51.0% |

Тобто «жар» — це лік згадок міст, а не активність біля цілі. Топ-1 продукту,
«Склад БК (Sevastopol)» 697, — це 697 постів зі словом «Севастополь».

**Що зроблено (коміт нижче).** Полагоджено те, що видно на КАРТІ: рядок
«⌖ поблизу: X (N км)» у попапі ([makeraid.py](makeraid.py:628)) більше не
називає один обʼєкт як повʼязаний із подією. Тепер сусідство рахується на
сторінці з `targets.json`: один обʼєкт у радіусі 15 км — називаємо (вибору
нема); кілька — число й типи, без імені; крапка-фолбек (`centroid`,
`region-snap`) рядка не отримує взагалі. Відстані в кілометрах нема — вона
рахувалась від центроїда НП, не від місця події. Сховище й `near` не чіпані,
перебудова не потрібна.

Заміряно на вересні (3747 крапок-днів): було 449 рядків (12.0%), стало 1909
(50.9%), з них 485 (25.4%) називають конкретний обʼєкт.

**Що лишилось і є продуктовим рішенням оператора.** Сам рейтинг цілей
(`rank_targets.py`, поле `hits`) і далі рахує лік згадок міст. Варіанти:
(A) прибрати жар із `imp`, лишити ярус; (B) показувати жар на рівні НП
(«Севастополь — 697 згадок, поруч 12 обʼєктів»); (C) рахувати лише привʼязки
з доказом (текст називає клас обʼєкта — 1.5% подій; або в радіусі рівно одна
ціль — 9.6%). Це периферія (див. CLAUDE.md, «Пріоритет»), без запиту не брати.

Окремо, дешево й не зроблено: `REGIONAL_FALLBACK` із `region` усередині
використовує ще `site.py:440` — «найглибша точка доби» відкидає повноцінні
розвʼязання. Зачіпає зведення на сайті, не карту.

---

## 2. 2410 подій (1.2%) стоять на підконтрольній Україні

Карта про удари по РФ, а крапки лежать на Миколаївщині й Чернігівщині.
Перевірка: `territory.depth_km(lat, lon) == 0`.

| скільки | місце | типовий текст |
|---|---|---|
| 416 | Mykolayiv Oblast | «Противник готовит ещё запуски БПЛА **от** Николаевской области» |
| 330 | Chernihiv Oblast | «Противник готовит запуски БПЛА **от** Черниговской области» |
| 234 | Odeska Oblast | «…от Одесской и Николаевской» |
| 109 | Kharkiv Oblast | «Харьковская область РФ авиационная…» |
| 98 | Dnipropetrovsk Oblast | «Днепропетровск / Самолёт противника» |
| 89 | Chernihiv | «Ещё пуски БПЛА от Чернигов в направлении Брянской» |
| 78 | Kupyansk | «Купянск и далее в тыл ракетная тревога» |
| 34 | Kozacha Lopan | **«Казачья бухта / Пролет БПЛА»** — це Севастополь, помилка розвʼязання |

Переважно це **джерело пусків**, а не місце події. Правило «`от X` — джерело»
вже є ([`store.py` SRC_BEFORE](tgmine/store.py:481)), але воно діє лише коли
топонім не перший у пості, а тут він якраз перший.

**Пропозиція.** Не тягнути ці події в точковий шар карти взагалі (окремий
`scope`, напр. «джерело»), або відсівати координати на підконтрольній Україні
для каналів моніторингу РФ. Рішення продуктове — питати оператора.
Окремо: «Казачья бухта» -> Козача Лопань це просто помилка геокода, 34 події.

---

## 3. Залишок точкових подій на центрах областей: 5958 (12.7%)

Розклад **після** правок, по причинах:

| причина | подій | що це |
|---|---|---|
| району в тексті нема | 4750 | справжня межа методу: пост сказав лише область |
| **району нема в газетирі** | **844** | дірка в даних |
| район у НЕ першому рядку | 202 | `freeform` читає лише перший (`lines: 1`) |
| **район Є у своїй області, але не знайшовся** | **103** | **баг, причина невідома** |
| район знайдено в чужій області | 59 | мітка області хибна (див. §5, §6) |

**103 — це баг**, і він не розібраний. Район існує в газетирі, лежить у
названій області, а розвʼязання не сталось. Почати варто саме з них: витягнути
десяток прикладів і подивитись, на якому кроці губиться.

**844 відсутні райони** (найчастіші): Чернский 116, Ершичский 95, Вяземский 95,
Колпнянский 75, Узловский 72, Дубенский 69, Ельнинский 53, Верховский 26,
Турковский 21, Краснинский 18, Ярцевский 18, Сызранский 16. Це дірка в
GeoNames. Лікувати можна лише новим джерелом або курованим списком райцентрів —
рішення оператора, бо це нове джерело даних.

**202 у другому рядку** — правиться зміною `lines: 1` у `configs/ru-monitor.yaml`.
Але обережно: `lines: 1` стоїть свідомо, бо в цих каналів рядок 1 — географія, а
далі йде тип загрози й реклама. Розширення на 2 рядки треба міряти окремо.

---

## 4. Родовий відмінок не доходить до ключа району

[`geocode.py` ADJ_SUFFIX](tgmine/geocode.py:36) має
`ский|ская|ское|ские|цкий|цкая|цкое|ской|скому|ском`, але **не має `ского`**
(і `ских`, `ским`, `скую`, `цкого`, `цкой`).

Наслідок: `_stem("валуйского")` = `валуйск`, а ключ «валуйский район» дає стем
`валуй` — вони не зустрічаються в жодному кошику. «Валуйского района»,
«Погарского», «Почепского», «Кстовского», «Ровеньским», «Сампурского»,
«Лужского» гинуть так само.

Заміряно: **485 із 41 477 (1.2%)** згадок «X район» не доходять до району саме
через відмінок. `ADJ_FULL` (широкий корінь) ці форми знає — розходяться саме
два індекси.

**Пастка.** Правка на кілька символів у регулярці, але вона міняє стемінг
**усьому геокоду**, не лише районам. Обовʼязково обидва арбітри (див. §12) і
розмічений набір `tests/data/homonyms.jsonl`.

---

## 5. Субʼєктів РФ нема в конфігу — пост дістає ЧУЖУ область

Якщо субʼєкта нема в `entities.регіон`, пост отримує першу **знайдену**
область, і мітка виходить хибна. Заміряно постів, де єдина географія — такий
субʼєкт:

Чувашія 426, Адигея 384, Марій Ел 242, Калмикія 63, Північна Осетія 13,
Інгушетія й Чечня (в переліках Кавказу), Псковська (≈12 подій із хибною
міткою «Ленінградська»).

Координату вони мають (після `a0ea78b` цілий субʼєкт позначається як ПЛОЩА і
крапкою не стає), але `region` у події — чужий, і всі зведення по областях
через це брешуть.

**Пропозиція.** Додати ці субʼєкти в `configs/ru-monitor.yaml`. Це не нове
джерело даних, лише конфіг. Але: кожен доданий патерн треба міряти на всьому
корпусі — `CLAUDE.md` має кілька історій, де доданий маркер перекидав сотні
постів за тисячу кілометрів («Кировск\w*» -> Крим, «Новотроицк\w*» -> Херсонщина).

---

## 6. «Борисоглебск» — маркер Воронезької, а район є в Ярославській

49 подій. Пост «Сигорь, Борисоглебский район, Ярославская область» дістає
мітку «Воронезька», бо `Борисоглебск\w*` стоїть маркером Воронезької.
Крапка після правок стає правильною (Ярославський Борисоглібський), а підпис
області — ні.

Той самий клас, що §5, але тут не відсутність субʼєкта, а колізія імен.
Лікується або звуженням патерна (`(?![\s\S]{0,60}Ярослав)` — як уже зроблено
для «Красноармейский»), або додаванням Ярославської в маркери. Міряти.

---

## 7. Одна область — один запис на пост, решта збігів губиться

[`extract.entities_of`](tgmine/extract.py:151) дедуплікує за
`(тип, значення, модифікатор)`. Тому з поста лишається `match` ЛИШЕ першого
збігу області.

«Курская область / Курск / Район Автовокзала» — перший збіг це «Курская»
(назва області, після `6dabbc9` вона не маркер), і місто **Курськ** із того
самого поста не потрапляє нікуди. Подія лишається на центроїді.

**Не заміряно.** Порахувати, у скількох постах другий збіг тієї самої області
є справжнім містом, і чи варто лишати найінформативніший збіг замість першого.

---

## 8. Події «по площі» позначаються адмінцентром

66 558 подій (32% усіх) мають `geo_conf` `centroid`/`region-snap`; у 63 517 із
них відома область, і вони стоять у
координатах адмінцентру з `configs/ru-monitor.yaml`. Карта малює їх підсвіткою
області, не крапкою, тож болить менше — але ця ж координата йде в метрику
глибини (`depth`).

Адмінцентр далеко від середини області: Тюменська 224 км, Свердловська 203,
Костромська 185, ЯНАО 179, Ростовська 134, ХМАО 132, Омська 125, Татарстан 123,
Пермський 123, Ставропольський 109, Астраханська 108, Воронезька 106.
Подій, де зсув >80 км — **10 417**.

**Пропозиція.** Для подій по площі брати центроїд полігона з `regions.json`, а
адмінцентр лишити для маркерів. Перевірити, що не поламається `depth` і
санітарна межа 400 км у `store._event`.

---

## 9. 897 точкових подій (1.9%) на найслабшому фолбеку

`geo_conf="global"` — «найбільший однойменний за населенням», без жодного
контексту. Було 9149 по всьому сховищу, стало 7243 після правок; у точковому
шарі лишилось 897. Це не баг, а відома стеля — але на карті така крапка
виглядає так само впевнено, як решта.

**Пропозиція.** Показувати інакше (напівпрозоро) або не показувати зовсім.
Продуктове рішення.

---

## 10. «возможно на X» не продовжує ланцюг напрямків

[`store.py` ENUM_GAP](tgmine/store.py:447) = `[\s,\-–—/]*(?:или|и)?[\s,\-–—/]*`.
Проміжок «, возможно на » під нього не підпадає, тож ланцюг «в направлении
Долгоруково, Задонск, возможно на Елец» обривається, і **Єлець стає місцем
події замість напрямку**.

Приклад: «Фиксации БПЛА над Семилукским районом, Воронежской области в
направлении Долгоруково, Задонск, возможно на Елец, Липецкая область».

Дрібниця за обсягом, але правиться одним словом у регулярці. Міряти.

---

## 11. Внутрішньоміські райони великих міст

«Кировский район», «Ворошиловский район» — це райони МІСТА Волгограда, і в
газетирі їх нема. `lookup` бере тезку за 380 км. По 3 події кожен, але клас
загальний: у кожного мільйонника такі райони є.

**Пропозиція.** Якщо пост назвав область і «X район», а району в цій області
нема — не брати далекого тезку, лишати центр області. Чесніше за вигадану
крапку.

---

## 12. Як це все міряти (обовʼязково прочитати перед правкою)

Без цього будь-яке число тут буде хибним — перевірено на собі тричі.

**Два арбітри, бо одного мало.**

1. **Полігони Natural Earth** (`regions.json`) — чи крапка лежить у полігоні
   ВЛАСНОЇ області поста. Незалежний від геокода: той працює на кодах ADM1
   GeoNames, це інша база. Але він карає за те, що крапка пішла з *хибно
   приписаної* області, хоч місце правильне.
2. **Відстань до району, який пост ПРЯМО назвав** — істина з тексту, не з
   мітки області. Брати пости, де «X район» резолвиться в НАЗВАНІЙ області, і
   міряти відстань від крапки до нього. Обережно з читанням: крапка, що
   переїхала на СЕЛО з того самого поста, за цим арбітром «стала далі», хоч
   насправді точніша — таких було 2979 із 3464.

**Стенд А/Б.** Сторона «до» береться з `git show <base>:` окремим пакетом
(`tgmine_old/`) і СВОЇМ `point_entity`, а не поточним. Стенд, що рахує обидві
сторони новим кодом, показує рівно нуль і виглядає як «нічого не зламав» —
найправдоподібніший вигляд зламаного заміру.

**Кожну зміну міряти ще й ІЗОЛЬОВАНО.** Три правки разом дали +3592, а
«`от X` для НП» окремо — лише +28. Без окремого заміру вона сховалась би за
великою.

**Лік на СПІЛЬНИХ id, парами рядок-до-рядка.** Словник за `id` дає нетотожні
множини «до» й «після» — так я двічі опублікував хибні числа. І не складати
два послідовні заміри: подія, що зрушила на обох кроках, лічиться двічі.

**Регреси роздивлятись поіменно.** У них щоразу сидить ІНША вада, а не шум:
саме так знайшлись §1, §2 і більшість цього списку.

**Порядок робіт із сховищем.** Спершу `git pull --rebase --autostash`, тоді
`python3 sync.py --rebuild`, тоді коміт. Навпаки не тримається: CI щогодини
комітить `sync.py --since 2d`, і дві свіжі доби лишаються від старого розбору —
саме ті, які оператор дивиться першими. Контроль: другий `--rebuild` поспіль
має дати `git status` без змін.

**Після перебудови сховища — підняти ОБИДВІ версії кешу** в
`.github/workflows/update.yml`: `raids-vN` і `rank-vN`, і в ключі `key:`, і в
`restore-keys:`, і у відновленні, і в збереженні. Розбіжність між restore і
save я вже одного разу зробив — жар рахувався б щогодини з перезаливанням
0.8 МБ ×24 на добу.

**`targets.json` належить CI.** Локальний перерахунок туди не комітити.
