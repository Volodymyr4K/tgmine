# Версії закріплені точно, а не через >=.
#
# Причина не абстрактна: збирач ходить у мережу щопівгодини без нагляду, а
# beautifulsoup4 розбирає HTML Telegram. Мінорний реліз може тихо змінити
# поведінку парсера — і дізнаєшся про це не з падіння збірки, а з підозріло
# тихих ночей у даних, коли пости є, але не розбираються.
#
# Оновлювати свідомо: підняти версію, прогнати
#   python3 -m unittest discover -s tests -t .
#   python3 sync.py --rebuild
# і звірити, що події не змінились.
requests==2.32.5
beautifulsoup4==4.14.3
PyYAML==6.0.3
pyshp==3.1.4
